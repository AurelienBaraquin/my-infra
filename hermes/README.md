# Hermes Configuration

Sanitized Hermes Agent configuration (API keys and tokens redacted).

## Files
- `config.yaml` — Main Hermes config (sensitive values replaced with `<REDACTED>`)

## Restoring
1. Copy `config.yaml` to `~/.hermes/config.yaml`
2. Re-enter your API keys and secrets
3. Restart the Hermes gateway
